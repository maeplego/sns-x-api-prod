global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: sns-x-api-prod
    metrics_path: /metrics
    static_configs:
      # Host-run API (Windows Docker Desktop / Mac Docker Desktop)
      - targets: ["host.docker.internal:8002"]
        labels:
          service: sns-x-api-prod
