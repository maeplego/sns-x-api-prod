# Learning articles (historical)

These Markdown files are **copied from the learning/product series** (`sns-x-api`).
They explain feed/ranking concepts and older tutorial ports (`:8000` / `:5174`) and version numbers.

They are **not** the source of truth for this production fork’s ports, versions, or ops.
Prefer the root [README](../README.md) and [`docs/`](../docs/) for current behavior.

Do not treat version headers inside these articles as the package version of `sns-x-api-prod`.
